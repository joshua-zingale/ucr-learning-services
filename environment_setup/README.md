This is a Python script that aids in the management of the env_vars.txt and
hosts.txt files scattered throughout the repository.

# Usage

The script can be run with

```bash
uv run environment-setup
```

and the help menu can be shown with

```bash
uv run environment-setup -h
```