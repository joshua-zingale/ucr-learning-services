`confenvflag` is a small package that extends Go's standard library's `flag`
package with the ability to set arguments from environment variables or a config
file in addition to as command-line arguments.

## Usage

This package supplies one function, `Parse()`, the use of which should be
understood by reading the documentation string in the source code.
